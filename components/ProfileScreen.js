import React from 'react';
import { View, Text } from 'react-native';

const ProfileScreen = () => {
    return (
        <View>
            <Text>Hello</Text>
        </View>
    );
};

export default ProfileScreen;
